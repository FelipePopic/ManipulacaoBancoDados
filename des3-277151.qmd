---
title: "Desafio 3"
author: "Felipe Pedroso Popic"
format: html
---

```{r setup, include=FALSE}
library(jsonlite)
library(arrow)
```

```{r, eval==FALSE}
# JSON -------------------------

df_json <- fromJSON("../dados/iris.json")
df_json <- as.data.frame(df_json)

print(df_json)

```

```{r, eval=FALSE}

# 1) Ler o JSON
df <- fromJSON("../dados/iris.json")
df <- as.data.frame(df)   # garantir que é data.frame
# 2) Conferir se veio certo
print(df)
str(df)
# 3) Salvar em Parquet
write_parquet(df, "dados.parquet")
# 4) Ler de volta
df_parquet <- read_parquet("dados.parquet")
print(df_parquet)

```